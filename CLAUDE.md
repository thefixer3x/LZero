# vortexai-l0

AI/ML infrastructure app.

## Commands (run from this directory)

- `bun run dev`
- `bun run build` — `build:browser` / `build:node` for target-specific builds
- `bun run test` — `test:watch`, `test:coverage`
- `bun run lint`
- `bun run type-check`
